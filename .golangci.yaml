linters:
  enable-all: true

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - funlen
        - gomnd
        - gocognit
        - scopelint
    - path: doc_test\.go
      linters:
        - lll
